model:
  variant: small   # small|medium|large|melody
  device: auto     # auto|cuda|mps|cpu
generation:
  use_sampling: true
  top_k: 250
  top_p: 0.0
  temperature: 1.0
  cfg_coef: 3.0
  duration_default: 30.0
  num_samples_per_seed: 1
output:
  out_root: runs
  run_name: musicgen_small_promptsv1_dur10_cfg3_topk250
  save_raw_wav: true
  save_loudness_wav: true
  save_spectrogram: true
  spectrogram_type: mel
